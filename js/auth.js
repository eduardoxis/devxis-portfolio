import {auth,firebaseReady} from '../firebase/firebase-config.js';import {signInWithEmailAndPassword,onAuthStateChanged,getIdTokenResult} from 'https://www.gstatic.com/firebasejs/10.14.1/firebase-auth.js';
const form=document.querySelector('#login-form'),toast=document.querySelector('#toast');const say=message=>{toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3500)};if(!firebaseReady)say('Configure o Firebase antes de fazer login.');form?.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('button');try{button.disabled=true;const user=await signInWithEmailAndPassword(auth,form.email.value.trim(),form.senha.value);const token=await getIdTokenResult(user.user,true);if(token.claims.admin!==true){await auth.signOut();throw new Error('Acesso não autorizado.')}location.href='dashboard.html'}catch(error){say(error.message||'Não foi possível entrar.')}finally{button.disabled=false}});
